<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login | AI StudyPal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/login.css"> <!-- ✅ External CSS -->
</head>
<body class="bg-light d-flex flex-column min-vh-100">

  <!-- ✅ Success Message -->
  <% if (success && success.length > 0) { %>
    <div class="alert alert-success text-center rounded-0 mb-0">
      <%= success %>
    </div>
  <% } %>

  <!-- ❌ Error Message -->
  <% if (error && error.length > 0) { %>
    <div class="alert alert-danger text-center rounded-0 mb-0">
      <%= error %>
    </div>
  <% } %>

  <div class="container py-5">
    <h1 class="text-center mb-4">🔑 Login</h1>

    <!-- Login Form -->
    <form action="/login" method="POST" class="mx-auto p-4 shadow rounded bg-white" style="max-width: 500px;">
      <div class="mb-3">
        <label for="username" class="form-label fw-bold">Username</label>
        <input type="text" id="username" name="username" class="form-control" required>
      </div>

      <div class="mb-3">
        <label for="password" class="form-label fw-bold">Password</label>
        <input type="password" id="password" name="password" class="form-control" required>
      </div>

      <div class="d-grid mb-3">
        <button type="submit" class="btn btn-success">Login</button>
      </div>
    </form>

    <!-- OR Divider -->
    <div class="text-center my-3">
      <hr>
      <span class="bg-light px-2 text-muted">OR</span>
    </div>

    <!-- Google Login Button -->
    <div class="text-center">
      <a href="/auth/google" class="google-btn mx-auto d-flex align-items-center justify-content-center">
        <img class="google-icon" src="https://img.icons8.com/?size=100&id=17949&format=png&color=000000" alt="Google Logo">
        <span class="btn-text">Continue with Google</span>
      </a>
    </div>

    <!-- Signup Redirect -->
    <div class="text-center mt-3">
      Don’t have an account? <a href="/signup" class="fw-bold text-decoration-none">Register here</a>
    </div>
  </div>

  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

</body>
</html>
